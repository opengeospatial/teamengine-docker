pipeline {
  agent any
  tools {
    maven 'mvn'
    jdk 'JDK 8'
  }
  stages {
    stage('Preparation') {
      steps{
        deleteDir()
        sh 'git clone git@github.com:opengeospatial/teamengine-docker.git .'
      }
    }
    stage('Build') {
      steps{
        sh 'mvn clean package docker:build'
      }
    }
  }
}
